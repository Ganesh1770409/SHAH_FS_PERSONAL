# Copy to .env for local runs. On Render/Railway, set these in the dashboard instead.
SECRET_KEY=use-a-long-random-string-here
# Optional: PostgreSQL from your host (Render adds DATABASE_URL automatically)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
